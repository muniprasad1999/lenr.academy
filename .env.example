# Vite environment variables
# Copy this file to .env.local and customize as needed

# Analytics Configuration
# Analytics (Umami) are automatically disabled in:
#   - Development mode (MODE=development)
#   - CI/test environments (VITE_CI=true)
# They only load in production when user consent is accepted
# Set VITE_CI=true in CI/CD pipelines to prevent test traffic from being tracked
VITE_CI=

# API endpoint (if using external data source)
# VITE_API_URL=https://api.example.com

# Database configuration
# VITE_DB_URL=/data/nanosoft.db

# Sentry Error Tracking (Production only)
# Get your DSN from: https://sentry.io/settings/projects/
# Leave empty to disable error tracking
VITE_SENTRY_DSN=https://89b0f10c88c093a3030a476b65d19b7c@o4510161719656448.ingest.de.sentry.io/4510161726079056

# Sentry Source Map Upload (CI/CD only)
# These are only needed for the build process in CI/CD
# Get auth token from: https://sentry.io/settings/account/api/auth-tokens/
# Organization slug: found in Sentry URL (e.g., https://sentry.io/organizations/YOUR-ORG/)
# Project slug: found in Sentry project settings
SENTRY_ORG=episkpos
SENTRY_PROJECT=lenr-academy
SENTRY_AUTH_TOKEN=
